# See /docs/CLAUDE.md

This is a symlink-equivalent pointer file. The single source of truth for
this project's standards is `docs/CLAUDE.md` at the repo root. Read that
file, not this one — this file exists only so the `.claude/` folder layout
matches Section 2 of the constitution.
